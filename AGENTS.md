# AGENTS.md — راهنمای اجرایی ایجنت هوش مصنوعی

**نسخه:** ۲.۰ — بازنویسی بر اساس بازبینی انتقادی  
**آخرین به‌روزرسانی:** ۱۴۰۵-۰۵-۱۶

---

## ۰) الزام اولیه پیش از هر تسک (غیرقابل صرف‌نظر)

قبل از شروع هر تسک، ایجنت **باید** مراحل زیر را به ترتیب انجام دهد و خروجی مرحله اول را در پاسخ خود quote کند:

1. جدول «رجیستر وضعیت مراحل» در `WORDPRESS_CONVERSION_EXECUTION_PLAN.md` را بخوان.
2. وضعیت فعلی همه مراحل را در پاسخ خود به شکل خلاصه بنویس (مثال: «WP-01 تا WP-03: تکمیل شده، WP-04: در حال انجام، بقیه: انجام نشده»).
3. مرحله مرتبط با تسک فعلی و وابستگی‌های آن را مشخص کن.
4. تأیید کن که همه وابستگی‌ها به وضعیت `تکمیل شده` رسیده‌اند. اگر نرسیده‌اند، **کار را متوقف کن** و به کاربر اعلام کن.

> **قاعده تضمین اجرا:** اگر ایجنتی این مرحله را انجام ندهد، نتیجه کارش معتبر نیست و باید از ابتدا تکرار شود.

---

## ۱) تعریف «تسک» (برای جلوگیری از scope creep)

یک تسک **دقیقاً** یکی از موارد زیر است:

| نوع تسک | تعریف | مثال |
|---|---|---|
| **مرحله WP-*** | اجرای یک مرحله کامل از جدول رجیستر | اجرای WP-01 |
| **زیرتسک** | اجرای بخش مشخصی از یک مرحله که در طول اجرا تعریف شده | «ساخت فقط `setup.php` از WP-03» |
| **رفع باگ** | حل یک خطای مشخص بدون تغییر در ساختار | «رفع خطای PHP Fatal در functions.php» |
| **بازبینی** | خواندن و گزارش بدون تغییر | «بررسی خطاهای کنسول JS» |

هر تسکی که در این چهار دسته نگنجد، ابتدا باید با کاربر شفاف‌سازی شود.

---

## ۲) بایدها

### ۲.۱) حفظ ظاهر و ساختار (با معیارهای قابل اندازه‌گیری)

- باید ظاهر فعلی قالب از نظر چیدمان، ابعاد، رنگ‌ها، فونت‌ها، انیمیشن‌ها و رفتارهای تعاملی حفظ شود.
- باید ترتیب DOM سکشن‌ها مطابق نسخه فعلی باقی بماند.
- باید کلاس‌ها و `id`های وابسته به CSS/JS فقط در صورت ضرورت و با ثبت اثرات تغییر داده شوند.

**معیار «نشکستن ظاهر» (قابل اندازه‌گیری):**
- تفاوت رنگ: بیشتر از ۵ واحد در فضای رنگی HSL مجاز نیست
- تفاوت ابعاد: بیشتر از ۲px انحراف در عناصر layout مجاز نیست
- انیمیشن‌ها: باید همه transition/animation هایی که در قالب فعلی وجود دارند در خروجی نهایی نیز حضور داشته باشند
- روش تأیید: مقایسه screenshot سمت دسکتاپ (۱۴۴۰px) و موبایل (۳۹۰px) با ابزار مقایسه بصری یا بازبینی دستی

### ۲.۲) استانداردهای وردپرس

- باید از ساختار استاندارد قالب وردپرس استفاده شود.
- باید `get_header()`, `get_footer()`, `get_template_part()` و APIهای رسمی وردپرس به‌کار گرفته شوند.
- باید CSS/JS/Fonts از طریق `wp_enqueue_style` و `wp_enqueue_script` بارگذاری شوند.
- باید خروجی‌های پویا با توابع Escape مناسب وردپرس ایمن شوند (`esc_html`, `esc_attr`, `esc_url`, `wp_kses_post`).
- باید ورودی‌های مدیریتی با `sanitize_*` توابع وردپرس sanitize و validate شوند.
- باید سازگاری با وردپرس نسخه **۶.۴+** و PHP نسخه **۸.۱+** حفظ شود.
- باید همه فرم‌ها از `wp_nonce_field()` / `wp_verify_nonce()` استفاده کنند.
- باید جلوی اجرای مستقیم فایل‌های include گرفته شود (با بررسی `defined('ABSPATH')`).

### ۲.۳) مدیریت‌پذیری از پنل

- باید بخش‌های ثابت هدف به واحدهای قابل مدیریت از پنل وردپرس تبدیل شوند.
- باید برای هر ناحیه قابل مدیریت، روش مناسب انتخاب شود (راهنمای انتخاب در بخش ۵).
- باید دامنه آزادی کاربر طوری طراحی شود که ظاهر اصلی قالب نشکند.
- باید همه قابلیت‌های مدیریتی جدید در `WORDPRESS_CONVERSION_EXECUTION_PLAN.md` مستند شوند.

### ۲.۴) کنترل کیفیت

- باید بعد از هر تغییر، اثر آن روی UI دسکتاپ (۱۴۴۰px) و موبایل (۳۹۰px) بررسی شود.
- باید بعد از هر تغییر، خطاهای PHP (`WP_DEBUG=true`) و JS (کنسول مرورگر) بررسی شوند.
- باید سالم بودن لینک‌ها، anchorها، فرم‌ها و دارایی‌ها بررسی شود.
- باید هر رگرسیون ظاهری یا رفتاری فوراً رفع یا با دلیل مستند و ثبت‌شده به تسک بعدی موکول شود. 
- باید **Admin Bar وردپرس** (وقتی لاگین است) ظاهر قالب را نشکند.

### ۲.۵) مستندسازی و رهگیری

- باید هر مرحله با همان شناسه `WP-*` پیگیری شود.
- باید فقط از وضعیت‌های زیر استفاده شود: `انجام نشده` | `در حال انجام` | `تکمیل شده` | `نیاز به بازبینی`
- باید اگر مرحله‌ای کامل نشد، دلیل آن شفاف ثبت شود.
- باید اگر تصمیم معماری جدیدی گرفته شد، آن تصمیم در مستند برنامه ثبت شود.

### ۲.۶) امنیت (الزامی در همه مراحل)

- باید همه `include` / `require` ها دارای بررسی `defined('ABSPATH') || exit;` باشند.
- باید هیچ ورودی کاربر بدون sanitize در query یا خروجی استفاده نشود.
- باید همه AJAX handler ها `wp_verify_nonce()` داشته باشند.
- باید فرم‌های تماس دارای honeypot field یا rate limiting (با transient وردپرس) باشند.
- باید مختصات نقشه و اطلاعات تماس از طریق `wp_localize_script` به JS پاس داده شوند (نه hardcode در JS).

---

## ۳) نبایدها

- نباید ظاهر قالب را به بهانه بهینه‌سازی یا ساده‌سازی تغییر دهی.
- نباید بدون ثبت در مستند، ساختار مرحله‌ها یا وابستگی‌ها را عوض کنی.
- نباید داده mock را در خروجی نهایی وردپرس باقی بگذاری مگر موقت و صریحاً ثبت‌شده.
- نباید بارگذاری مستقیم و پراکنده CSS/JS را در صفحات نهایی وردپرس حفظ کنی.
- نباید از الگوهای ناسازگار با وردپرس مثل `$_SERVER['PHP_SELF']` برای تشخیص صفحه فعال استفاده کنی (در وردپرس از `is_front_page()`, `is_page()` و غیره استفاده کن).
- نباید محتوای قابل‌مدیریت را hard-code نگه داری وقتی در برنامه تبدیل آن پیش‌بینی شده است.
- نباید بدون تست، مرحله‌ای را `تکمیل شده` علامت بزنی.
- نباید تغییرات نامرتبط با مرحله فعال را بدون نیاز واقعی وارد کنی.
- نباید بدون توجه به سازگاری Block Editor (وردپرس ۵.۸+)، ویجت‌های کلاسیک PHP پیاده‌سازی کنی.
- نباید `wp_mail()` را بدون مکانیزم logging یا اطلاع‌رسانی به کاربر پیاده کنی.
- نباید CDN های خارجی (Tailwind, Swiper, Google Fonts) را در خروجی نهایی production به صورت `<link>` یا `<script>` مستقیم در HTML باقی بگذاری.

---

## ۴) پروتکل حل تعارض

### ۴.۱) تعارض بین «حفظ ظاهر» و «استانداردهای وردپرس»

اگر اجرای یک استاندارد وردپرس ناگزیر باعث تغییر ظاهری شود:
1. **ابتدا** راه‌حل غیرمخرب را بررسی کن (مثلاً: ثبت Tailwind CDN از طریق enqueue به جای inline — حفظ کارکرد، رعایت استاندارد).
2. اگر راه‌حل غیرمخرب وجود ندارد، **متوقف شو** و به کاربر گزینه‌ها را با تأثیرات آن‌ها ارائه بده.
3. بدون تأیید صریح کاربر، تغییر ظاهری انجام نده.

### ۴.۲) تعارض بین دستور کاربر و قوانین AGENTS.md

اگر کاربر دستوری بدهد که با قوانین این فایل تعارض دارد:
1. تعارض را به وضوح به کاربر اعلام کن.
2. توضیح بده که اجرای دستور چه ریسکی دارد.
3. منتظر تأیید صریح کاربر بمان.
4. اگر کاربر تأیید کرد، با ثبت تصمیم در مستند، اجرا کن.

### ۴.۳) تعارض بین Widget کلاسیک و Block Editor

- اگر پروژه روی وردپرس ۵.۸+ مستقر می‌شود، برای سازگاری باید `add_theme_support('widgets-block-editor', false)` اضافه شود **یا** از Customizer API استفاده شود.
- تصمیم باید در WP-12 ثبت و برای WP-13 اجرا شود.

---

## ۵) راهنمای انتخاب روش مدیریت محتوا

| نوع محتوا | روش ترجیحی | دلیل |
|---|---|---|
| اطلاعات تماس، شبکه‌های اجتماعی، مختصات نقشه | Customizer API یا Theme Options | داده سراسری، یکبار تعریف |
| منوی ناوبری | `wp_nav_menus` وردپرس | استاندارد، قابل ویرایش از پنل |
| سکشن‌های تکرارشونده با آیتم‌های مشابه (خدمات، مزایا) | Custom Post Type + Loop | ساختاریافته، قابل مقیاس |
| سکشن‌های ایستای قابل ویرایش (Hero, CTA) | Customizer API | ساده‌تر از CPT، کافی برای داده‌های کم |
| بلوک‌های قابل جابه‌جایی | Widget Area (با `add_theme_support('widgets-block-editor', false)`) | نیازمند Classic Widget API |
| مقالات، اخبار | Post Type استاندارد وردپرس + Taxonomy | استاندارد کامل |
| فوتر | چند Sidebar مجزا یا Theme Options | بسته به پیچیدگی محتوا |

---

## ۶) قواعد تصمیم‌گیری اجرایی

### ۶.۱) اگر بین چند روش پیاده‌سازی مردد بودی

روشی را انتخاب کن که:
1. به استاندارد وردپرس ۶.x نزدیک‌تر باشد
2. ظاهر فعلی را دقیق‌تر حفظ کند
3. نگهداری بلندمدت ساده‌تری داشته باشد
4. بیشترین قابلیت مدیریت از پنل را با کمترین احتمال شکستن UI فراهم کند
5. سازگار با PHP 8.1+ باشد

### ۶.۲) اگر `wp_mail()` استفاده می‌کنی

- نتیجه (true/false) را log کن یا در ادمین نمایش بده.
- به کاربر توضیح بده که برای محیط production به پلاگین SMTP (مثل WP Mail SMTP) نیاز است.
- هرگز فرض نکن که mail ارسال شده است.

### ۶.۳) اگر یک تغییر باعث خطر رگرسیون ظاهری می‌شود

- ابتدا راه‌حل غیرمخرب‌تر را بررسی کن.
- اگر ناچار به تغییر ساختاری شدی، باید معیار تطابق بصری و تست جبرانی همان مرحله را ثبت کنی.
- تغییر را در یک branch جداگانه انجام بده (اگر Git در پروژه فعال است).

### ۶.۴) اگر با Tailwind کار می‌کنی

- Tailwind Play CDN در production مجاز نیست.
- باید از تصمیم WP-00 (استراتژی Tailwind) پیروی کنی.
- اگر از Tailwind CLI استفاده می‌شود، باید `safelist` برای کلاس‌های arbitrary `[value]` تعریف شده باشد.

---

## ۷) چک‌لیست اجباری قبل از بستن هر تسک

**الزامی — همه موارد باید تأیید شوند:**

- [ ] آیا مرحله مرتبط در `WORDPRESS_CONVERSION_EXECUTION_PLAN.md` پیدا و به‌روزرسانی شد؟
- [ ] آیا وضعیت مرحله صحیح ثبت شد؟
- [ ] آیا گزارش تکمیل‌شده‌ها و باقی‌مانده‌ها همگام شد؟
- [ ] آیا UI اصلی حفظ شده است؟ (مقایسه screenshot دسکتاپ ۱۴۴۰px و موبایل ۳۹۰px)
- [ ] آیا خطای PHP با `WP_DEBUG=true` وجود ندارد؟
- [ ] آیا خطای JS در کنسول مرورگر وجود ندارد؟
- [ ] آیا لینک‌ها و دارایی‌ها سالم هستند؟ (404 وجود ندارد)
- [ ] آیا Admin Bar وردپرس ظاهر را نشکسته؟
- [ ] آیا همه `include` ها دارای `defined('ABSPATH') || exit;` هستند؟
- [ ] آیا قابلیت مدیریتی جدید، مستند شده است؟

---

## ۸) قاعده نهایی

هر اجرا باید پروژه را یک گام واقعی و **قابل تأیید** به سمت «قالب وردپرس استاندارد، قابل مدیریت از پنل، و منطبق با ظاهر فعلی» نزدیک‌تر کند. گام‌های بدون تست، بدون مستندسازی یا بدون تأیید بصری، پیشرفت محسوب نمی‌شوند.
